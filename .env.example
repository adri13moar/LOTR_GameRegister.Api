# Copy to `.env` and fill in real values. `.env` is gitignored.
MSSQL_SA_PASSWORD=TuPasswordSeguro123!
JWT_KEY=CHANGE_ME_AT_LEAST_32_CHARS_RANDOM
# For the opencode/agent machine (NOT used by docker compose): get a free key at context7.com/dashboard
CONTEXT7_API_KEY=
