# LOTR Game Register

## SQL Queries

In the following examples, ensure to use:

```sql
SELECT h.Id, h.Name, COUNT(*)
FROM Hobbit h
GROUP BY h.Id, h.Name;
```

## .NET Version

### English Section

The current version of .NET used is **10**.

### Spanish Section

La versión actual de .NET utilizada es **10**.